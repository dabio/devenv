---

- hosts: lifebook
  become: yes
  roles:
  - {role: packages, tags: ["packages", "fish", "tmux", "git"]}
  - {role: kubernetes, tags: ["kubernetes", "helm"]}
  - packages
  - gnome
  - {role: vim, tags: ["vim"]}
  - {role: golang, tags: ["golang"]}
